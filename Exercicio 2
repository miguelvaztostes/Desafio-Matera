Exemplo em Python

num = int(input("Digite um número: "))

# Definir as duas primeiras posições da sequência
fib = [0, 1]

# Loop para calcular os próximos valores da sequência
while fib[-1] < num:
    next_val = fib[-1] + fib[-2]
    fib.append(next_val)

    # Verificar se o número informado já foi encontrado
    if next_val == num:
        print(f"O número {num} pertence à sequência de Fibonacci!")
        break
else:
    print(f"O número {num} não pertence à sequência de Fibonacci.")

***EXPLICANDO O CÓDIGO***
Primeiro, pedimos ao usuário que informe um número através da função input() e convertemos o valor para um número inteiro com a função int().
Em seguida, definimos as duas primeiras posições da sequência de Fibonacci como 0 e 1.
Utilizamos um loop while para calcular os próximos valores da sequência. A expressão fib[-1] < num significa que o loop deve continuar enquanto o último valor da lista fib for menor que o número informado.
Dentro do loop, calculamos o próximo valor da sequência como a soma dos dois últimos valores da lista fib e adicionamos o valor na lista com o método append().
Verificamos se o número informado já foi encontrado na sequência comparando o valor recém calculado com o número informado. Se for igual, exibimos uma mensagem informando que o número pertence à sequência e saímos do loop com o comando break.
Se o loop terminar normalmente sem encontrar o número, exibimos uma mensagem informando que o número não pertence à sequência. O bloco else é executado quando o loop não é interrompido pelo comando break
