CREATE TABLE REVISAO_CONTROLE (
  COD_REVISAO NUMBER(2) PRIMARY KEY,
  DESCRICAO VARCHAR2(30) NOT NULL
);

INSERT INTO REVISAO_CONTROLE (COD_REVISAO, DESCRICAO)
VALUES (1, 'Precisa revisar');
INSERT INTO REVISAO_CONTROLE (COD_REVISAO, DESCRICAO)
VALUES (2, 'Revisão em 3 meses');
INSERT INTO REVISAO_CONTROLE (COD_REVISAO, DESCRICAO)
VALUES (3, 'Revisão em 6 meses');
INSERT INTO REVISAO_CONTROLE (COD_REVISAO, DESCRICAO)
VALUES (4, 'Não precisa revisar');

